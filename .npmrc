# npm workspaces hoisted most deps to the root; pnpm isolates them by default.
# Hoisted layout keeps TypeScript and bundlers able to resolve transitive types
# (e.g. csstype for @types/react, @radix-ui/* internals, react-router for RRD).
node-linker=hoisted
