{
  "name": "r2-upload-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for generating presigned URLs for R2 file uploads",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env development",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "test": "wrangler dev --test"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "r2",
    "presigned-url",
    "file-upload"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.59.3"
  }
}
